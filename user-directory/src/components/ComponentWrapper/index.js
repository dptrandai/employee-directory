import React from "react";

// main component wrapper to contain all componenets
function ComponentWrapper(props) {
    return <main className = "wrapper" {...props}/>
}

export default ComponentWrapper;
